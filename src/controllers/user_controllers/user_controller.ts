import { Request, Response } from "express";
import { prisma } from "../../database/prisma";

// const getAllUser = async (req : Request,resp : Response) => {
//     const userID = '';//hardcoded for now

//     const userGroups = await prisma.group.findMany({
//         where:{
            
//         }
//     })
// }